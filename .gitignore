# Node modules
/node_modules

# Compilation output
/dist

# pnpm deploy output
/bundle

# Hardhat Build Artifacts
/artifacts

# Hardhat compilation (v2) support directory
/cache

# Foundry (development-only, used for branch coverage)
/forge-out
/cache_forge

# Typechain output
/types

# Environment files
.env
.env.*
!.env.example

# Hardhat coverage reports
/coverage

# Snapshot cheatcodes output
/snapshots

# NOTE: `.openzeppelin/` and `gas-snapshot.json` are deliberately NOT ignored.
# The former is the source of truth for every proxy deployment and upgrade;
# the latter is the committed gas baseline.

.omc

# Ralph loop bookkeeping, not part of the template
/progress.txt

# Foundry coverage output
/lcov.info

# Local EDR deployment output (ephemeral; real networks are committed per fork)
/deployments/hardhatMainnet/
/deployments/hardhatOp/
